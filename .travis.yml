language: php

php:
  - 5.6
  - 7.0
  - 7.1
  - nightly

before_script:
  - composer install

script:
  - php vendor/bin/phpunit
  - php vendor/bin/phpcs library tests --standard=build/phpcs/Joomla --report=summary --extensions=php -np
